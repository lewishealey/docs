import React from "react"

const Title = ({children}) => (
    <div>
        <div style={{
            padding: "20px 0 20px 0",
            color: `#6F6F6F`,
            fontSize: 16,
            lineHeight: 1,
            textAlign: `left`,
            fontWeight: `bold`,

        }}>{children}</div> 
    </div>
)


export default Title
